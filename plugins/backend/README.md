# Backend Plugin

Backend part of the FinOps dynamic plugin.

## Run locally

From this directory:

```bash
yarn install
yarn start
```

## Notes

- Source code lives in `src/`.
- Build outputs are in `dist/` and `dist-dynamic/`.
